'use client';
import React from 'react'

const AddToList = () => {
  return (
    <div>
      <button onClick={()=>console.log("Add To List")}>Add To List</button>
    </div>

  )
}

export default AddToList